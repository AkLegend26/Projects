function cost = get_cost(quantity)
    regular_price = 20;
    disconut_price = 2;
    if quantity >= 20
        cost = quantity * discount price;
    else
        cost = quantity * regular_price;
    end
end